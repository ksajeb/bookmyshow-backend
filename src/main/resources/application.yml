
spring:
  application:
    name: bms

  datasource:
    url: jdbc:postgresql://localhost:5432/bookmyshow
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect